
    //html elements in javascript
    var node= document.createElement("p");
    var content=createTextNode("this is file para");
    node.appenChild(content);
    var root=document.getElementById("root");
    root.appenChild(root);
    //html elements in react
    const elment=react.createElement(h1,{},);
   // const node=react.createTextNode("this is heading from react");

    
